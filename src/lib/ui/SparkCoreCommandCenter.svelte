<script lang="ts">
  import SparkButton from './SparkButton.svelte'; import SparkCard from './SparkCard.svelte'; import SparkIcon from './SparkIcon.svelte'; import SparkTrustBadge from './SparkTrustBadge.svelte';
  import { sparkModules } from '$content/spark-content';
  import { getCompletedLessonCount, getLearningProgressPercent, getReadinessScore, getRecommendedLessonSlug, getRecommendedModuleId, getTotalLessonCount, learningState } from '$state/learning-state.svelte';
  const recommendedModule = $derived(sparkModules.find((module)=>module.id===getRecommendedModuleId()) ?? sparkModules[0]);
  const recommendedLesson = $derived(getRecommendedLessonSlug());
</script>
<section class="core-command-center"><div class="core-command-copy"><span class="spark-eyebrow">Spark Core</span><h1>Pusat kurikulum blockchain, Web3, dan Starknet.</h1><p>Mulai dari pemahaman sederhana, naik ke wallet safety, lalu masuk Praktik di Lab, Passport, dan Hub secara bertahap.</p><div class="core-command-actions"><SparkButton href={`/lesson/${recommendedLesson}`}>Lanjut Lesson</SparkButton><SparkButton href="#curriculum" variant="secondary">Lihat Kurikulum</SparkButton></div></div><aside class="core-command-panel"><div class="core-command-topline"><SparkTrustBadge label="Kurikulum utama" tone="beta" /><SparkTrustBadge label={learningState.experience === 'explorer' ? 'Penjelajah' : learningState.experience === 'guided' ? 'Terarah' : 'Pemula'} tone="local" /></div><SparkCard class="core-next-card"><span><SparkIcon name="book-open" size={22} /></span><div><small>Rekomendasi sekarang</small><strong>{recommendedModule.title}</strong><p>{recommendedModule.subtitle}</p></div></SparkCard><div class="core-stat-row"><div><strong>{getCompletedLessonCount()}/{getTotalLessonCount()}</strong><span>Lesson</span></div><div><strong>{getLearningProgressPercent()}%</strong><span>Belajar</span></div><div><strong>{getReadinessScore()}%</strong><span>Readiness</span></div></div></aside></section>
