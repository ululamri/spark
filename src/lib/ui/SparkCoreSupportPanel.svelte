<script lang="ts">
  import SparkCard from './SparkCard.svelte'; import SparkIcon from './SparkIcon.svelte'; import { coreDestinations, coreSupportCards } from '$lib/core/core-rich-model';
</script>
<div class="core-support-layout"><section><div class="support-section-head"><span class="spark-eyebrow">Bantuan jalur</span><h2>Jangan biarkan pengguna salah masuk jalur.</h2><p>Pemula bisa mulai pelan, pengguna berdasar punya jalan cepat, dan penjelajah punya ruang teknis.</p></div><div class="core-support-grid">{#each coreSupportCards as card}<a href={card.href}><SparkCard tone={card.tone}><span class="support-icon"><SparkIcon name={card.icon} size={19} /></span><h3>{card.title}</h3><p>{card.copy}</p><em>›</em></SparkCard></a>{/each}</div></section><section><div class="support-section-head"><span class="spark-eyebrow">Setelah Core</span><h2>Core bukan akhir. Core adalah pintu masuk.</h2><p>Setelah fondasi terbentuk, pengguna bergerak ke praktik, Passport, komunitas, dan Hub.</p></div><div class="core-destination-grid">{#each coreDestinations as item}<a href={item.href}><span><SparkIcon name={item.icon} size={17} /></span><div><strong>{item.title}</strong><small>{item.copy}</small></div></a>{/each}</div></section></div>
