<script lang="ts">
  import SparkButton from './SparkButton.svelte'; import SparkCard from './SparkCard.svelte'; import SparkIcon from './SparkIcon.svelte'; import SparkTrustBadge from './SparkTrustBadge.svelte';
  import { betaSession } from '$state/beta-session-state.svelte'; import { getLearningProgressPercent, getReadinessScore, getRecommendedLessonSlug } from '$state/learning-state.svelte';
  const nextHref = $derived(betaSession.user ? '/dashboard' : '/login');
</script>
<section class="home-product-stage"><div class="home-stage-main"><span class="spark-eyebrow">Karyra Spark</span><h1>Belajar, praktik, komunitas, Passport, dan Hub dalam satu gateway.</h1><p>Spark bukan hanya halaman belajar. Spark adalah alur aplikasi untuk membantu pengguna lokal memahami blockchain dan Starknet secara aman.</p><div class="home-stage-actions"><SparkButton href={nextHref}>{betaSession.user ? 'Buka Dashboard' : 'Coba Akun Contoh'}</SparkButton><SparkButton href={`/lesson/${getRecommendedLessonSlug()}`} variant="secondary">Lesson Rekomendasi</SparkButton></div></div><aside class="home-stage-console"><div class="home-console-head"><SparkTrustBadge label={betaSession.user ? 'Session aktif' : 'Guest'} tone={betaSession.user ? 'safe' : 'beta'} /><SparkTrustBadge label="Frontend beta" tone="local" /></div><div class="home-console-grid"><SparkCard><SparkIcon name="dashboard" size={20} /><strong>Dashboard</strong><small>Ruang kerja harian</small></SparkCard><SparkCard><SparkIcon name="book-open" size={20} /><strong>Core Beginner</strong><small>Kurikulum utama</small></SparkCard><SparkCard><SparkIcon name="badge" size={20} /><strong>{getReadinessScore()}%</strong><small>Passport readiness</small></SparkCard><SparkCard><SparkIcon name="compass" size={20} /><strong>{getLearningProgressPercent()}%</strong><small>Progress belajar</small></SparkCard></div></aside></section>
